# nodejs-project .npmrc
# Force npm to ignore optional platform-specific sharp prebuilts when possible.
# The postinstall script (scripts/strip-native-sharp.js) handles the final
# cleanup if any still sneak in.
fund=false
audit=false
optional=true
